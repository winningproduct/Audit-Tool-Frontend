import { environment } from '@environments/environment';

export const productRoute: string = environment.host + '/product';
export const productPhaseRoute: string = environment.host + '/productPhase';
export const knowledgeAreaRoute: string = environment.host + '/knowledgeArea';
export const evidenceRoute: string = environment.host + '/evidence';
export const questionRoute: string = environment.host + '/question';
export const userRoute: string = environment.host + '/user';
export const adminRoute: string = environment.host + '/admin';
